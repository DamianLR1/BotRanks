const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Bot en funcionamiento');
});

app.listen(3000, () => {
  console.log('Servidor keep-alive escuchando en puerto 3000');
});